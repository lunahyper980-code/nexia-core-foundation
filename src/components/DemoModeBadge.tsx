import { useDemoMode } from '@/contexts/DemoModeContext';
import { useUserRole } from '@/contexts/UserRoleContext';
import { MonitorPlay } from 'lucide-react';

export function DemoModeBadge() {
  // Badge disabled - demo mode is silent
  return null;
}
